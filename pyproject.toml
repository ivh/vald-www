[project]
name = "vald-www"
version = "1.0.0"
description = "VALD Web Interface - Django replacement for PHP app"
requires-python = ">=3.11"
dependencies = [
    "django>=5.0",
    "gunicorn>=21.0",
]

[dependency-groups]
dev = []
